rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /daylyMissions/{id} {
      allow read: if true;
      allow create: if request.auth.uid == request.resource.data.uid;
      allow update: if request.auth.uid == resource.data.uid;
    }
  }
}
